import styled from "styled-components";

export const FooterCustom = styled("footer")({
  display: "flex",
  width: "100%",
  alignItems: "center",
  justifyContent: "center",
  padding: "64px 0",
});
