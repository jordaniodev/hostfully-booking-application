import styled from "styled-components";

export const ContainerListReserve = styled('ul')({
    all: 'unset',
    listStyle: 'none',
    display: 'flex',
    flexDirection: 'column',
    gap: '1rem',
    marginTop: '2rem'
})

export const ContainerButton = styled('div')({
    marginTop: '2rem',
})